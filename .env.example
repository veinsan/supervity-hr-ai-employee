# Local seed-loader credentials
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional integration references used by the Supervity Auto workspace.
# Keep real values in Supervity's credential store or your local .env only.
SLACK_BOT_TOKEN=
TYPEFORM_WEBHOOK_SECRET=
